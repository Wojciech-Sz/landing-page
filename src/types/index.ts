export type IconProps = {
  className: string;
};

export type ContactFormProps = {
  firstName: string;
  secondName: string;
  phoneNumber: string;
  email: string;
  subject: string;
  message: string;
};
